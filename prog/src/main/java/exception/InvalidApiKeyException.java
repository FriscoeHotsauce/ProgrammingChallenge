package exception;

/**
 * A descriptive exception class that extends RuntimeException.
 * Used to denote that our REST call failed due to an invalid API key
 */
public class InvalidApiKeyException extends RuntimeException {
}
